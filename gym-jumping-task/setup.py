from setuptools import setup

setup(name='gym_jumping_task',
      version='0.0.1',
      install_requires=['gym',
                        'argparse',
                        'numpy',
                        'pygame'
      ]
)